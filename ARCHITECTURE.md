# Architecture Overview - Prismic Multi-Locale Blog

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         User's Browser                           │
│                                                                   │
│  ┌──────────────────┐                                            │
│  │ Accept-Language  │ (e.g., "ja-JP,ja;q=0.9,en-US;q=0.8")      │
│  └────────┬─────────┘                                            │
└───────────┼──────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Next.js Middleware                          │
│                     (middleware.ts)                              │
│                                                                   │
│  ┌────────────────────────────────────────────────────┐         │
│  │  1. Detect user's preferred language                │         │
│  │  2. Check current URL path                          │         │
│  │  3. Redirect if needed:                             │         │
│  │     - Japanese user → /ja/blog                      │         │
│  │     - Other users → /blog                           │         │
│  └────────────────────────────────────────────────────┘         │
└───────────┼──────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Next.js App Router                          │
│                                                                   │
│  ┌──────────────────┐         ┌──────────────────┐              │
│  │   /blog          │         │   /ja/blog       │              │
│  │   (English)      │         │   (Japanese)     │              │
│  │                  │         │                  │              │
│  │  - page.tsx      │         │  - page.tsx      │              │
│  │  - [slug]/       │         │  - [slug]/       │              │
│  │    page.tsx      │         │    page.tsx      │              │
│  └────────┬─────────┘         └────────┬─────────┘              │
└───────────┼──────────────────────────────┼────────────────────────┘
            │                              │
            └──────────────┬───────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Prismic Blog Library                          │
│                  (src/lib/prismic-blog.ts)                       │
│                                                                   │
│  ┌────────────────────────────────────────────────────┐         │
│  │  getBlogPosts(locale)                              │         │
│  │  getBlogPostByUid(uid, locale)                     │         │
│  │  getAllBlogPostUids()                              │         │
│  │  getAlternateLocalePosts(uid, locale)              │         │
│  └────────────────────────────────────────────────────┘         │
└───────────┼──────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Prismic Client                               │
│                     (prismicio.ts)                               │
│                                                                   │
│  ┌────────────────────────────────────────────────────┐         │
│  │  - Repository configuration                        │         │
│  │  - Route resolvers                                 │         │
│  │  - Cache strategy                                  │         │
│  └────────────────────────────────────────────────────┘         │
└───────────┼──────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Prismic API                                 │
│              (your-repo.cdn.prismic.io)                          │
│                                                                   │
│  ┌──────────────────┐         ┌──────────────────┐              │
│  │  English Content │         │ Japanese Content │              │
│  │  (locale: en-us) │         │ (locale: ja-jp)  │              │
│  │                  │         │                  │              │
│  │  - blog_post 1   │         │  - blog_post 1   │              │
│  │  - blog_post 2   │         │  - blog_post 2   │              │
│  │  - blog_post 3   │         │  - blog_post 3   │              │
│  └──────────────────┘         └──────────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. Blog Listing Page Request

```
User visits /blog
    │
    ├─→ Middleware checks language
    │   └─→ Japanese? Redirect to /ja/blog
    │
    ├─→ Page component renders
    │   └─→ getBlogPosts('en-us')
    │       └─→ Prismic Client fetches
    │           └─→ Returns array of posts
    │
    └─→ Render blog cards with:
        - Featured images
        - Titles
        - Descriptions
        - Tags
        - Dates
```

### 2. Blog Post Detail Request

```
User visits /blog/my-post
    │
    ├─→ generateStaticParams() pre-generates all UIDs
    │
    ├─→ generateMetadata() creates SEO tags
    │   └─→ getBlogPostByUid('my-post', 'en-us')
    │       └─→ Returns post data
    │           └─→ Generate hreflang, OG tags, JSON-LD
    │
    ├─→ Page component renders
    │   ├─→ getBlogPostByUid('my-post', 'en-us')
    │   ├─→ getAlternateLocalePosts('my-post', 'en-us')
    │   └─→ Render:
    │       - Featured image
    │       - Title & metadata
    │       - Rich text content
    │       - Language switcher
    │       - Social sharing
    │
    └─→ Static page served (ISR)
```

## Component Hierarchy

```
Page Component
│
├─── LanguageSwitcher
│    └─── Shows available locales
│
├─── Breadcrumbs
│    └─── Navigation trail
│
├─── Article
│    ├─── Featured Image (Next.js Image)
│    ├─── Header
│    │    ├─── Title
│    │    ├─── Date (locale-formatted)
│    │    └─── Tags
│    │
│    ├─── PrismicContent
│    │    └─── Rich text with custom components
│    │         ├─── Headings
│    │         ├─── Paragraphs
│    │         ├─── Images
│    │         ├─── Code blocks
│    │         └─── Links
│    │
│    └─── RelatedPosts
│         └─── Similar articles
│
└─── Footer
     ├─── Back to blog link
     └─── Social sharing buttons
```

## Locale Resolution Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    URL Pattern Matching                      │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
   /blog/post          /ja/blog/post          /:lang/blog/:uid
        │                     │                     │
        ▼                     ▼                     ▼
   locale: en-us        locale: ja-jp         Dynamic locale
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              Prismic Query with Locale Filter                │
│                                                               │
│  client.getByUID('blog_post', uid, { lang: 'en-us' })       │
│  client.getByUID('blog_post', uid, { lang: 'ja-jp' })       │
└─────────────────────────────────────────────────────────────┘
```

## SEO Metadata Generation

```
Blog Post Data
    │
    ├─→ generateSEOMetadata()
    │   ├─→ Title tag
    │   ├─→ Meta description
    │   ├─→ Canonical URL
    │   ├─→ Open Graph tags
    │   │   ├─→ og:locale (en_US or ja_JP)
    │   │   ├─→ og:alternate_locale
    │   │   ├─→ og:image
    │   │   └─→ og:type (article)
    │   ├─→ Twitter Card tags
    │   └─→ Hreflang tags
    │       ├─→ en: /blog/post
    │       ├─→ ja: /ja/blog/post
    │       └─→ x-default: /blog/post
    │
    └─→ generateArticleJSONLD()
        └─→ Schema.org BlogPosting
            ├─→ headline
            ├─→ description
            ├─→ author
            ├─→ datePublished
            ├─→ dateModified
            └─→ inLanguage (en-US or ja-JP)
```

## Caching Strategy

```
┌─────────────────────────────────────────────────────────────┐
│                    Production Build                          │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
  Static Pages          ISR Pages            Dynamic Data
        │                     │                     │
        ├─ /blog              ├─ /blog/[slug]       ├─ Prismic API
        ├─ /ja/blog           └─ /ja/blog/[slug]    │
        │                           │               │
        │                           ▼               │
        │                    Revalidate: 5s        │
        │                    (development)          │
        │                    force-cache            │
        │                    (production)           │
        │                                           │
        └───────────────────────┬───────────────────┘
                                │
                                ▼
                         Served from CDN
                         (Lightning fast!)
```

## File Organization

```
fuminozawa-portfolio-2.0/
│
├── src/
│   ├── app/
│   │   ├── blog/
│   │   │   ├── page.tsx (English listing)
│   │   │   └── [slug]/
│   │   │       └── page.tsx (English detail)
│   │   │
│   │   └── ja/
│   │       └── blog/
│   │           ├── page.tsx (Japanese listing)
│   │           └── [slug]/
│   │               └── page.tsx (Japanese detail)
│   │
│   ├── components/
│   │   ├── PrismicContent.tsx
│   │   ├── LanguageSwitcher.tsx
│   │   ├── Breadcrumbs.tsx
│   │   └── RelatedPosts.tsx
│   │
│   └── lib/
│       ├── prismic-blog.ts (Data fetching)
│       ├── locales.ts (Locale utilities)
│       └── seo.ts (SEO helpers)
│
├── prismicio.ts (Client config)
├── middleware.ts (Locale detection)
└── slicemachine.config.json (Prismic config)
```

## Key Features Map

```
Feature                    Implementation
─────────────────────────────────────────────────────────────
Multi-locale               ✓ locales.ts + middleware.ts
Auto-detection             ✓ middleware.ts (Accept-Language)
Hreflang tags              ✓ seo.ts (getAlternateUrls)
Language switcher          ✓ LanguageSwitcher.tsx
Rich text rendering        ✓ PrismicContent.tsx
Featured images            ✓ Next.js Image component
SEO optimization           ✓ generateSEOMetadata()
JSON-LD                    ✓ generateArticleJSONLD()
Static generation          ✓ generateStaticParams()
ISR support                ✓ Prismic client config
Responsive design          ✓ Tailwind CSS
Social sharing             ✓ X/Twitter, LinkedIn buttons
```

---

This architecture provides:
- 🚀 **Fast performance** with static generation
- 🌍 **Global reach** with multi-locale support
- 🔍 **SEO excellence** with proper metadata
- 🎨 **Beautiful design** with modern UI
- 📱 **Responsive** across all devices
- ♿ **Accessible** with semantic HTML
